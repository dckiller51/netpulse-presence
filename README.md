# netpulse-presence
Event-driven device presence detection for OpenWrt (Wi-Fi + wired), with multi-AP support via a central tracker. Publishes standard HA MQTT-discovery device_tracker entities — no custom component needed.
